services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    image: slim-gemini-agent:local
    env_file:
      - .env
    volumes:
      - gemini_home:/home/agent/.gemini
    restart: unless-stopped

volumes:
  gemini_home:
