# superpos-agent-core is the shared runtime — installed from PyPI so this repo
# can build standalone.  Compatible-release pinned so patch updates (e.g. the
# model-on-heartbeat support in 0.1.1) are picked up automatically.
superpos-agent-core~=0.1

# Transitive deps of superpos-agent-core are listed here for IDE/lockfile
# clarity even though pip would pull them in via core's own pyproject.toml.
python-telegram-bot>=21.0
httpx>=0.27.0
pyyaml>=6.0
