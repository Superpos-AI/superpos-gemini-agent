# superpos-agent-core is the shared runtime — pulled from GitHub so this repo
# can build standalone.  Pin to a specific commit/tag for reproducible
# builds; @main is fine during active development.
superpos-agent-core @ git+https://github.com/Superpos-AI/superpos-agent-core.git@505b443

# Transitive deps of superpos-agent-core are listed here for IDE/lockfile
# clarity even though pip would pull them in via core's own pyproject.toml.
python-telegram-bot>=21.0
httpx>=0.27.0
pyyaml>=6.0
